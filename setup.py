from setuptools import setup, find_packages

setup(name='pyics',
      version='0.1',
      description='python package to implement industrial control system',
      packages=find_packages(),
      )
      

